plugins {
    id 'java'
    id "com.github.ben-manes.versions" version "0.22.0"
 }

allprojects {
    repositories {
        jcenter()
    }
}

subprojects {
    version = '1.0'
}

apply plugin: 'java'


defaultTasks 'test', 'shadowJar'
